#include<iostream>
#include<vector>
#include<string>
#include<algorithm>
using namespace std;
#include "function.h"
 
int main(){
     //---------- VECTORS --------------//
   
     // vector<int> v;
     // int n;
     // cout<<"Enter the size of the array "<<endl;
     // cin>>n;
     // for(int i = 0 ; i < n ; i++){
     //      int x;
     //      cout<<"Enter "<<i+1<<" element "<<endl;
     //      cin>>x;
     //      v.push_back(x); // O(1)
     // }
     // printVec(v);

     // vector<int> v;
     // v.push_back(7);
     // v.push_back(8);

     // vector<int> v2 = v;  // copy bana dega O(n) time complexity me
     // v2.push_back(5);
     // printVec(v);
     // printVec(v);
     // printVec(v2);


     //---------- VECTOR OF PAIR ----------//
     // vector<pair<int,int>> vec = {{1,2},{2,3},{3,4}};
     // vector<pair<int,int>> vec;
     // int n;
     // cout<<"Enter the number of element : "<<endl;
     // cin>>n;
     // for(int i = 0 ; i < n ; i++){
     //      int x,y;
     //      cin>>x>>y;
     //      vec.push_back({x,y});
     // }
     // printVec2(vec);

     //-------------- ARRAY OF VECTOR --------------//
     // int N;
     // cin>>N;
     // vector<int> vec[N];

     // for(int i = 0 ; i<N ; i++){
     //      int n;
     //      cin>>n;
     //      for(int j = 0 ; j < n ; j++){
     //           int x;
     //           cin>>x;
     //           vec[i].push_back(x);
     //      }
     // }

     // for(int i = 0 ; i < N ; i++){
     //      printVec(vec[i]);
     // }
     

     //------------ VECTORS OF VECTORS ------------//
     // vector<vector<int>> vec;
     // int N;
     // cin>>N;
     // for(int i = 0 ; i<N ; i++){
     //      int n;
     //      cin>>n;
     //      // vector<int> temp;
     //      vec.push_back(vector<int>());
     //      for(int j = 0 ; j < n ; j++){
     //           int x;
     //           cin>>x;
     //           // temp.push_back(x);
     //           vec[i].push_back(x);
     //      }
     //      // vec.push_back(temp);
     // }
     // vec[0].push_back(100);

     // for(int i = 0 ; i < vec.size() ; i++){
     //      printVec(vec[i]);
     // }
// 3
// 3
// 1 2 3
// 2
// 4 5
// 3
// 6 7 8




     
return 0;
}