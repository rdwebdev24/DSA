// PAIR //
// basically it is a class used to store values on pair

#include<iostream>
#include<vector>
using namespace std;


int main(){
     // pair<int,string> p;
     // p = make_pair(2,"abc");
     // p = {2,"fasfsd"};
     // cout<<p.first<<endl;

     // pair<int,string> p1 = p; // copy hua hai bas
     // pair<int,string> &p1 = p; // by reference hua hai matlab copy nahi hua !!
     // p1.first = 3;
     // cout<<p.first<<" "<<p.second<<endl;

     //-------- ARRAY OF PAIR --------//
     // pair<int , int > p_arr[3];
     // p_arr[0] = {1,2};
     // p_arr[1] = {2,3};
     // p_arr[2] = {3,4};

     // for(int i = 0 ; i < 3 ; i++){
     //      cout<<p_arr[i].first<<" "<<p_arr[i].second<<endl;
     // }


     // pair<int , int> p;
     // pair<int , string> p;
     // pair<int , char> p;
     // pair<int , double> p;
     // pair<int , float> p;
     // cout<<p.first<<p.second<<endl;

     // SWAPING IN PAIR //
     // pair<int , string> p1;
     // pair<int , string> p2;
     // p1 = {1,"rohit"};
     // p2 = {2,"mummy"};
     // cout<<"Before swaping : "<<p1.second<<" "<<p2.second<<endl;
     // p1.swap(p2);
     // cout<<"after swaping : "<<p1.second<<" "<<p2.second<<endl;

     

     

return 0;
}