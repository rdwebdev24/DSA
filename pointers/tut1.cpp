#include<iostream>
using namespace std;
 
int main(){


     // int a = 1;
     // char b = 'h';
     // float c = 1.2;
     // int *p = &a;
     // char *q = &b;
     // float *r = &c;
     
     // cout<<sizeof(p)<<endl;
     // cout<<sizeof(q)<<endl;
     // cout<<sizeof(r)<<endl;    // size of any type of pointer is same as because 
                                  // the pointer are loaded in a addrss register in the 
                                  // processor so the size of the address register control the 
                                  // size of the pointer 

     // int i = 10;
     // int* p = &i;

     // cout<<p<<endl;
     // p = p+1;
     // cout<<p<<endl;


     // POINTERS IN ARRAY 

     // int a[10];
     // a[0] = 1;
     // a[1] = 3;

     // cout<<a<<endl;
     // cout<<&a[0]<<endl;  // a is basically equal to a[0]

     // cout<<a<<endl;
     // cout<<&a<<endl; 

     // cout<<*a<<endl;
     // cout<<*(a+1)<<endl;
     // cout<<sizeof(a)<<endl;


   
return 0;
}